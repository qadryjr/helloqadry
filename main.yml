---
 - name: Exercise3
   #hosts: ubuntu
   #Use the public IP if the VM is already created
   hosts: all
   user: ec2-user
   become: true
   become_method: sudo
   become_user: root
   roles:
    - setup
